import { StandardFilter } from './models';

/**
 * DEFAULT_FILTERS array to specify default filters
 * if not specified, return first filter
 */
export const DEFAULT_FILTERS: StandardFilter[] = [];
